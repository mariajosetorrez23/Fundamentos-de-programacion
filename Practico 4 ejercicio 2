#include <stdio.h>

int main() {
    int n, z = 0, dig;
    printf("Ingrese un numero: ");
    scanf("%d", &n);

    while (n > 0) {
        dig = n % 10;
        z = z * 10 + dig;
        n = n / 10;
    }

    printf("El numero invertido es: %d\n", z);
    return 0;
}
