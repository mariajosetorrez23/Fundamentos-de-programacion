#include <stdio.h>
int main(){
    int n,i,cont=0;
    printf("Ingrese N: ");
    scanf("%d",&n);
    for(i=1;i<=n;i++){
        if(n%i==0){
            cont++;
        }
    }
    if(cont==2) printf("Es primo");
    else printf("No es primo");
    return 0;
}
