#include <stdio.h>

int main() {
    int n, i, suma = 0;
    printf("Ingrese un numero N: ");
    scanf("%d", &n);

    for(i = 1; i <= n; i++) {
        suma += i * i;
    }

    printf("La suma de los cuadrados es: %d\n", suma);
    return 0;
}
