#include <iostream>
#include <string>
using namespace std;

int main(){
    string c1, c2;
    cout << "Cadena 1: ";
    getline(cin, c1);
    cout << "Cadena 2: ";
    getline(cin, c2);

    string mezcla = c1 + c2;

    cout << "Mezcla: " << mezcla;
    return 0;
}
